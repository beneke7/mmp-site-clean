% Loaded only by LaTeXML, via --preload in site/build.py. It is what makes
% \iflatexml true during the HTML build; under pdflatex the conditional is
% defined (false) by mmp.sty and this file is never read.
\newif\iflatexml
\latexmltrue
